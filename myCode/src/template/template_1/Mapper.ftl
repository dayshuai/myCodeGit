package ${mapperPackage};

import ${beanPackage}.${table.tableName?cap_first};
import ${basePackage}.BaseMapper;

public interface ${className?cap_first} extends BaseMapper<${table.tableName?cap_first}> {

}
